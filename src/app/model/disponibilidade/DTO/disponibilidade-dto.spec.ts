import { DisponibilidadeDTO } from './disponibilidade-dto';

describe('DisponibilidadeDTO', () => {
  it('should create an instance', () => {
    expect(new DisponibilidadeDTO()).toBeTruthy();
  });
});
